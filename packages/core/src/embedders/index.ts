/**
 * Embedder abstraction layer for VectorORM.
 *
 * Provides a consistent interface for text embedding models,
 * allowing the SDK to work with any embedding provider.
 */

export { Embedder } from './embedder';
