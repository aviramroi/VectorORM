{
  "name": "glyph-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@10.2.4",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "docs:generate": "typedoc"
  },
  "devDependencies": {
    "turbo": "^1.12.0",
    "typedoc": "0.28.16",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@xenova/transformers": "2.17.2"
  }
}
