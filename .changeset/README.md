# Changesets

This folder is managed by [@changesets/cli](https://github.com/changesets/changesets).

When making a code change, run `npm run changeset` to create a changeset describing your change and its semver impact.
